[project]
name = "aissert-cli"
version = "0.1.0"
description = "CLI tool for running AI security and quality tests using Giskard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "giskard[llm]>=2.16.2",
    "pandas>=2.2.3",
    "pyyaml>=6.0.2",
    "requests>=2.32.3",
]
